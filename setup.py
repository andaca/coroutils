from distutils.core import setup

setup(name='coroutils',
      version='1.0',
      description='utils for working with coroutines',
      packages=['coroutils'],
      long_description=open('README.md', 'rt').read(),
      )
